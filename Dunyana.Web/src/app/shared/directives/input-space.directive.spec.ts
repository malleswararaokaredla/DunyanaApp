import { InputSpaceDirective } from './input-space.directive';

describe('InputSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new InputSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
